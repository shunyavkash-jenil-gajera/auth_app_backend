/**
 * Environment Names Enum.
 * Replaces raw magic strings across app configuration and middlewares.
 */
export enum Environment {
  DEVELOPMENT = 'development',
  PRODUCTION = 'production',
  TEST = 'test',
}
